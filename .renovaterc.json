{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:best-practices", ":dependencyDashboard"],
  "packageRules": [
    {
      "automerge": true,
      "automergeStrategy": "merge-commit",
      "matchDepTypes": ["*"],
      "rangeStrategy": "pin",
      "recreateWhen": "always"
    },
    {
      "matchManagers": ["github-actions"],
      "matchDepTypes": ["action"],
      "matchPackageNames": ["terraform-linters/setup-tflint"],
      "extractVersion": "^v(?<version>.*)$"
    },
    {
      "description": "Match tflint_version input parameter",
      "matchManagers": ["github-actions"],
      "matchDepTypes": ["input"],
      "matchDepNames": ["tflint_version"],
      "extractVersion": "^v(?<version>.*)$",
      "matchPackageNames": ["terraform-linters/tflint"]
    }
  ],
  "platformAutomerge": true,
  "prConcurrentLimit": 0,
  "prHourlyLimit": 0
}
